machine:
  ruby:
    version: 2.3.3
  environment:
    ci: true
dependencies:
    pre:
      - gem install bundler -v 1.14.6
deployment:
    release:
        tag: /release-.*/
        owner: circleci
        commands:
            - bundle exec cap production deploy
    master:
        branch: master
        commands:
            - bundle exec cap staging deploy