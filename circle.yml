machine:
  ruby:
    version: 2.3.3
dependencies:
    pre:
      - gem install bundler -v 1.14.6
deployment:
    release:
        tag: /release-.*/
        owner: circleci
        commands:
            - cap production test
    master:
        branch: master
        commands:
            - bundle exec cap staging test